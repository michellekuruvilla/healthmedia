source "https://rubygems.org"

# GitHub Pages gem (bundles Jekyll + recommended plugins)
gem "github-pages", group: :jekyll_plugins

# For running Jekyll locally with newer Ruby versions
gem "webrick", "~> 1.8"

# ExecJS for JavaScript runtime (may want a newer version)
gem "execjs", "~> 2.9"

# Optional: specify Ruby version (if applicable)
# ruby "3.2.0"
